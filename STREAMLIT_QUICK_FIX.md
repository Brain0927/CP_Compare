# 🔧 Streamlit Cloud API Key 快速修復指南

**應用 URL**: https://mag498qrjyhtyk5xgzbnhk.streamlit.app/

---

## ⚡ 立即修復（3 步）

### 第 1 步：訪問應用管理

打開你的瀏覽器，訪問：
```
https://share.streamlit.io/
```

或直接在應用頁面上按右上角 ⋮ 菜單

---

### 第 2 步：進入 Secrets 管理

1. 點擊菜單 ⋮ → **Settings**
2. 左側選單 → **Secrets**
3. 點擊 **Edit in Streamlit Cloud** 按鈕

---

### 第 3 步：添加 API Key

在編輯器中粘貼以下配置：

```toml
GEMINI_API_KEY = "AIzaSyCyX0WKTyDq9ncOlNqLoL8S85ldgq8oEeg"
```

**⚠️ 將上面的 Key 替換為你自己的 Gemini API Key**

---

### 第 4 步：保存

1. 點擊頁面下方的 **Save** 按鈕
2. 應用自動重啟（10-30 秒）
3. 刷新應用頁面 → 功能恢復 ✨

---

## 🎯 驗證修復成功

### 方法 1：使用應用功能

1. 訪問 https://mag498qrjyhtyk5xgzbnhk.streamlit.app/
2. 嘗試爬蟲功能或搜索產品
3. 如果看到 AI 分析結果 → ✅ 成功

### 方法 2：查看應用日誌

1. 應用設置 → 向下滾動找 **Logs**
2. 檢查是否有錯誤信息
3. 如果看到「Gemini API 已連接」→ ✅ 成功

### 方法 3：測試 API 連接

應用右下角應該有一個 ℹ️ 信息框，顯示：
```
✅ Gemini API 已連接
```

如果顯示：
```
❌ Gemini API 未設定
```

表示 Secrets 未正確配置。

---

## 🔑 如何獲得 Gemini API Key

如果你沒有 API Key：

1. 訪問 [Google AI Studio](https://aistudio.google.com/app/apikey)
2. 點擊 **Create API Key**
3. 選擇「Create API key in new project」
4. 複製生成的 Key
5. 粘貼到 Streamlit Secrets 中

---

## ✅ 代碼已更新

**最新修改**（已自動推送到 GitHub）：

```python
# config/settings.py

# 現在支援 Streamlit Cloud Secrets
try:
    import streamlit as st
    GEMINI_API_KEY = st.secrets.get("GEMINI_API_KEY", os.getenv("GEMINI_API_KEY", ""))
except (ImportError, AttributeError):
    # 本地開發環境
    GEMINI_API_KEY = os.getenv("GEMINI_API_KEY", "")
```

**優勢**：
- ✅ 自動檢測 Streamlit Cloud 環境
- ✅ 優先使用 Secrets（Streamlit Cloud）
- ✅ 備用本地 .env 文件
- ✅ 無縫支援多個部署環境

---

## 🚨 如果還是不工作

### 檢查清單

- [ ] Secrets 已保存（看到「Your secrets have been saved」提示）
- [ ] API Key 有效（在 Google AI Studio 檢查）
- [ ] 應用已重啟（等待 30 秒後刷新）
- [ ] 瀏覽器快取已清除（Ctrl+Shift+Delete）

### 強制重啟應用

1. 應用設置 → **Reboot app**
2. 或訪問應用主頁 → 右上角菜單 → **Rerun** / **Restart**

### 檢查日誌中的錯誤

應用設置 → Logs 部分，查看：
```
KeyError: 'GEMINI_API_KEY'     ← Secrets 未設置
403 Forbidden                   ← API Key 無效
429 Too Many Requests           ← 配額已用完
```

---

## 📊 三個環境現在都支援了

| 環境 | API Key 來源 | 讀取方式 |
|------|-------------|---------|
| **Streamlit Cloud** ⭐ | Secrets | `st.secrets.get()` |
| **本地開發** | `.env` 文件 | `os.getenv()` |
| **Docker 服務器** | 環境變數 | `os.getenv()` |

所有環境**自動切換**，無需改動代碼！

---

## 🎉 完成後

修復完成後，你的應用將能夠：

✅ 爬取 Momo、PChome、Yahoo 等平台的產品  
✅ 使用 AI 分析產品特徵和性價比  
✅ 自動計算最佳購買推薦  
✅ 展示詳細的分析報告  

---

## 💡 最佳實踐提醒

| 情況 | 做法 |
|------|------|
| 本地開發 | 使用 `.env` 文件（已在 .gitignore） |
| GitHub 推送 | 無需 `.env`，代碼已推送 |
| Streamlit 部署 | 在 Secrets 中設置 API Key |
| Docker 部署 | 使用環境變數或 `.env` |

---

**現在就配置，3 分鐘內恢復應用功能！** ⚡

時間戳：2025年12月16日  
更新內容：config/settings.py 已支援 Streamlit Secrets
